<%= link_to "New Category", new_category_path %><br/>

<% @categories.each do |category| %>
  <%= category.name %> <%= link_to category_path(category), method: :delete  do %><i class="fas fa-trash"></i><% end %><br />
<% end %>
